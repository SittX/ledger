Use this to generate and migrate schema changes with drizzle-kit

- Run "npm run db:generate" command to generate the updated schema migration files.
- Validate the generated schema and compare if the generated schema files are correct.
- Run "npm run db:migrate" command to execute the migration files.
