Run "npm run db:generate" command to generate the updated schema migration files using drizzle-kit.
Validate the generated schema and compare if the generated schema file is correct.
Run "npm run db:migrate" command to execute the migration files.
